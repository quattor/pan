#
# variables need to be initialised before they are used
# (and they are case sensitive!)
#
# @expect=org.quattor.pan.exceptions.EvaluationException
#

object template variable1;

"/bug" = {
  abc = 1;
  Abc;
};

